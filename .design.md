# XDRONE — Design System

## Palette

| Token            | Hex / Value                  | Usage                              |
|------------------|------------------------------|------------------------------------|
| `--bg`           | `#eef0f4`                    | Page background                    |
| `--bg-2`         | `#e4e8ed`                    | Subtle surface variation           |
| `--border`       | `#b8bec8`                    | Component borders                  |
| `--border-dim`   | `rgba(184,190,200,0.35)`     | Faint separators                   |
| `--cyan`         | `#3db8c8`                    | Primary accent — interactive focus |
| `--cyan-dim`     | `rgba(61,184,200,0.14)`      | Hover / active highlight fill      |
| `--text-1`       | `#1a2030`                    | Primary text / data values         |
| `--text-2`       | `#505870`                    | Secondary / label text             |
| `--text-3`       | `#909ab0`                    | Muted / helper / section headers   |
| `--amber`        | `#c87828`                    | Setpoint markers / warnings        |
| `--green`        | `#38a060`                    | Nominal / positive state           |

## Three.js Scene

| Element           | Hex       |
|-------------------|-----------|
| Background / fog  | `#e8eaee` |
| Ground plane      | `#cdd1da` |
| Grid major        | `#a0a5b5` |
| Grid minor        | `#c4c8d4` |

## Drone Model (Three.js)

| Part              | Hex        | Notes              |
|-------------------|------------|--------------------|
| Body center       | `#0c1a3a`  | Dark navy          |
| Arms              | `#162444`  | Medium navy        |
| Motor housings    | `#0a1428`  | Very dark navy     |
| Props (CCW M1,M3) | `#2868a8`  | Lighter navy-blue  |
| Props (CW  M2,M4) | `#1a3868`  | Deeper navy        |
| Forward LED       | `#3db8c8`  | Cyan (UI accent)   |
| Velocity arrow    | `#3db8c8`  | Cyan               |
| Trajectory trail  | `#3db8c8`  | Cyan, 60% opacity  |
| Setpoint marker   | `#c87828`  | Amber wireframe    |

## Typography

- **UI labels** : Inter 300, ALL CAPS, letter-spacing 0.10–0.14em, color `--text-3`
- **Data values**: JetBrains Mono 300, tabular figures, color `--text-1`
- **Base label size** : 9–10 px
- **Data value size** : 11–13 px

## Panel / Frame Component

Panels have **no solid fill** — defined by four L-shaped corner markers only.
A very faint white tint (`rgba(255,255,255,0.30)`) separates panels from page BG.

```css
.frame {
  background-color: rgba(255, 255, 255, 0.30);
  /* 4 × 2 gradient lines = 4 corner brackets */
  background-image:
    linear-gradient(var(--cyan), var(--cyan)),  /* TL horizontal */
    linear-gradient(var(--cyan), var(--cyan)),  /* TL vertical   */
    linear-gradient(var(--cyan), var(--cyan)),  /* TR horizontal */
    linear-gradient(var(--cyan), var(--cyan)),  /* TR vertical   */
    linear-gradient(var(--cyan), var(--cyan)),  /* BL horizontal */
    linear-gradient(var(--cyan), var(--cyan)),  /* BL vertical   */
    linear-gradient(var(--cyan), var(--cyan)),  /* BR horizontal */
    linear-gradient(var(--cyan), var(--cyan));  /* BR vertical   */
  background-size:
    10px 1px, 1px 10px,
    10px 1px, 1px 10px,
    10px 1px, 1px 10px,
    10px 1px, 1px 10px;
  background-position:
    top left,    top left,
    top right,   top right,
    bottom left, bottom left,
    bottom right, bottom right;
  background-repeat: no-repeat;
}
```

## Layout Grid

```
┌──────────────────── topbar 44px ────────────────────────────┐
│  ◈ XDRONE    ● ●  RUN  HOLD  RST              T+ 00:00.00  │
├────────────┬──────────────────────────────┬─────────────────┤
│  190 px    │   flex 1 (3D viewport)       │   190 px        │
│  LEFT      │                              │   RIGHT         │
│            │                              │                 │
│  TARGET    │                              │  TELEMETRY      │
│  DIST      │                              │  VEHICLE        │
│  GAINS     │                              │                 │
└────────────┴──────────────────────────────┴─────────────────┘
```

## Button Interaction States

| State     | Border        | Text         | Background      |
|-----------|---------------|--------------|-----------------|
| Default   | `--border`    | `--text-2`   | transparent     |
| Hover     | `--cyan`      | `--cyan`     | `--cyan-dim`    |
| Active    | `--cyan`      | `#fff`       | `--cyan`        |
| Disabled  | `--border-dim`| `--text-3`   | transparent     |
| Toggle ON | `--amber`     | `--amber`    | amber-dim       |
